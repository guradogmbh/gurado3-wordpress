import { observer } from 'mobx-react-lite';
import React from 'react';
import useForm from './customHooks'; 

const democom = observer(({}) => { 
  return (
    <>
      <h5>Registrierte Kunden</h5> 
     </> 
     
  );
});
export default democom;  
